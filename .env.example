# =============================================================================
# QAnubis — Environment Variables
# Copy this file to .env: cp .env.example .env
# For local development, all defaults below work after: docker compose up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/qanubis"

# -----------------------------------------------------------------------------
# NextAuth
# -----------------------------------------------------------------------------
NEXTAUTH_SECRET="change-me-in-production-use-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth — optional, leave empty to disable Google login
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub OAuth — optional, leave empty to disable GitHub login
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# -----------------------------------------------------------------------------
# Object Storage (S3-compatible)
# Local dev: MinIO runs via docker compose (http://localhost:9001 for UI)
# Production: set STORAGE_ENDPOINT to your Cloudflare R2 / AWS S3 endpoint
# -----------------------------------------------------------------------------
STORAGE_PROVIDER="s3"
STORAGE_ENDPOINT="http://localhost:9000"
STORAGE_REGION="us-east-1"
STORAGE_ACCESS_KEY="minioadmin"
STORAGE_SECRET_KEY="minioadmin"
STORAGE_BUCKET="qanubis"

# -----------------------------------------------------------------------------
# Email (SMTP)
# Local dev: MailHog runs via docker compose (http://localhost:8025 for UI)
# Production: replace with your SMTP provider credentials (Brevo, Gmail, etc.)
# -----------------------------------------------------------------------------
EMAIL_PROVIDER="nodemailer"
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="QAnubis <noreply@qanubis.app>"
